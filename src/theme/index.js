import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#202020",
    },
    secondary: {
      main: "#373737",
    },
  },
  typography: {
    fontFamily: "roboto",
  },
});

export default theme;
